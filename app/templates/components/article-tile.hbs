<li div class="well col-sm-3 col-sm-offset-1">
  {{article.title}}
  by:
  {{article.author}}
  {{article.published}}
  <img src={{article.image}} alt={{article.title}}>
  <br>
  <br>
  <button {{action 'delete' article}}>Delete Article</button>
</li>
